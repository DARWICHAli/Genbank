import ui as ui
import numpy as np

class Functions:


    @classmethod
    def get_regions_choice(cls):
        print("getting region choice")
        # selected_regions = np.array([])
        # if(ui.Ui_MainWindow().actionProkaryota.isChecked()):
        #     selected_kingdoms = selected_kingdoms + "prokaryota"
        # if(ui.Ui_MainWindow().actionArchaea.isChecked()):
        #     selected_kingdoms = selected_kingdoms + "archaea"
        # if(ui.Ui_MainWindow().actionBacteria.isChecked()):
        #     selected_kingdoms = selected_kingdoms + "bacteria"
        # if(ui.Ui_MainWindow().actionEucaryota.isChecked()):
        #     selected_kingdoms = selected_kingdoms + "eukaryota"
        # print("kingdom choices are %s" %selected_kingdoms)
    
    
    
    

